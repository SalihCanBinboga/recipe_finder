name: recipe_finder
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # HTTP Package
  dio: ^5.1.1
  # JSON Annotation Package
  json_annotation: ^4.8.0
  # Hive Local Database Package
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # Provider Inherited Widget Declaration
  provider: ^6.0.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.1
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true
