name: domain
version: 0.0.2
publish_to: none

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # JSON Annotation Package
  json_annotation: ^4.8.0
  # Dependency Injection
  injectable:
    path: ./../../../injectable/injectable
  # Test Mocking
  mockito: ^5.4.0

  core:
    path: ../core

dev_dependencies:
  test: ^1.24.2
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.1
  injectable_generator:
    path: ./../../../injectable/injectable_generator